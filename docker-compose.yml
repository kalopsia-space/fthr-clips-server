services:
  fthr-clips-server:
    build: .
    restart: unless-stopped
    ports:
      - "100.86.191.16:18080:8080"
    env_file: .env
    volumes:
      - clips-data:/data
    user: "10001:10001"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

volumes:
  clips-data:
