services:
  fthr-clips-server:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:18080:8080"
    env_file: .env
    mem_limit: 1g
    pids_limit: 128
    volumes:
      - clips-data:/data
    user: "10001:10001"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

volumes:
  clips-data:
